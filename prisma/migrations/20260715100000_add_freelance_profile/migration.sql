-- The table and user.no_telp exist in the deployed database but were never
-- recorded in migration history. This migration intentionally synchronizes
-- that history without modifying already-existing user data.
SELECT 1;
